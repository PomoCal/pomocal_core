import SwiftUI

extension Color {
    static let appBackground = Color("AppBackground")
    static let sidebarBackground = Color("SidebarBackground")
    static let cardBackground = Color("CardBackground")
    
    // Fallback colors if assets not defined
    static let premiumDark = Color(red: 0.1, green: 0.1, blue: 0.12)
    static let premiumLight = Color(red: 0.98, green: 0.98, blue: 0.99)
}
